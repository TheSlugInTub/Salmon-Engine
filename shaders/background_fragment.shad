#version 330 core
out vec4 FragColor;

in vec2 TexCoord;
in vec3 FragPos;

uniform sampler2D texture1;

void main()
{
    vec4 texColor = texture(texture1, TexCoord);

    // Output the final color
    FragColor = texColor;
}
